{
  "name": "orioldeprius-github-io",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git checkout develop && npm run build && git checkout main && git pull origin main && git rm -r . && git checkout develop -- dist && rsync -av dist/ . && rm -rf dist && git add . && git commit -m 'Deploy latest build' && git push origin main && git checkout develop"
  },
  "dependencies": {
    "aos": "^2.3.4",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.15",
    "@vitejs/plugin-vue": "^5.2.1",
    "tailwindcss": "^4.0.15",
    "vite": "^6.2.1",
    "vite-plugin-vue-devtools": "^7.7.2"
  }
}
